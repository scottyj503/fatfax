export const isCodeSent = store => (store.resetPassword.isCodeSent);
export const isSendingCode = store => (store.resetPassword.isSendingCode);
export const isConfirmed = store => (store.resetPassword.isConfirmed);
export const isConfirming = store => (store.resetPassword.isConfirming);
export const error = store => (store.resetPassword.error);
